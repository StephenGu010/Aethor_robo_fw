# LCD 本地控制检查页（2026-09-09）

用户观察到电机黑色部位转动约20多度，暂不继续处理角度与速度语义。该观察不能确定转动部件属于转子或输出轴，也不构成60度到位验收。本阶段推进LCD浏览与控制准备流程，不进行电机运动。

## 本次行为

- 台架LCD默认选择ID7，并显示电机列表末页；七电机配置结构保留。
- 首页申请本地控制若被门禁拒绝，进入“本地控制检查”，显示模式校验、参数发现、反馈时效、POS标定配置标志和当前拒绝原因。
- 左键返回首页，右键进入所选电机详情，中键按当前快照重新检查。重新检查不主动发送CAN发现请求。
- 条件满足时仍进入原确认页，保留松开再长按确认流程；只读编译不能提交控制请求。
- 固件保持LCD-ReadOnly；未更改运动阈值、标定配置或电机参数。

## 软件验证

- 模型、编译期只读拦截、阻塞后导航及重新检查确认流程测试通过。
- 实际LVGL渲染24个不同280×240页面，字库检查通过；剩余LVGL内存27656字节。
- 按键方向、DMA期间STOP、显示恢复、模式确认页面和故障清理回归通过。
- Keil最终编译0错误、0警告，内存布局检查通过。
- HEX SHA256：`8b2c8a6ca97a01b5b225eb49d5c6b240901105032288fb8b6b2d5ea9cfd283ea`。

## 回退与硬件证据

修改前源码和HEX/AXF备份：`D:\download\TCG\output\can_id_commissioning_20260909\before-control-prepare-page.zip`。

刷写回读结果以同目录 `lcd-control-prepare-flash-result.json` 为准。界面截图为软件模拟数据，不能作为电机在线证明。固件复位会清除本次发现信息；显示未知或反馈过期时，不应推断电机故障。

本次实测：241508字节Flash回读一致；20次SWD采样中处理器持续运行，UI心跳从444增加到832，LCD状态READY，输入/DMA/SPI/初始化错误计数为0。USB返回 `enabled=00 moving=0 active=0 fault=none`，CAN错误和busoff均为0。本轮没有重新发现电机或试转，因此不据此声称电机反馈链路已验收。

下一阶段可继续完善只读状态与交互。开放LCD实机运动前仍需解决角度/速度语义与单电机标定，并验证反馈时效及到位判定。
