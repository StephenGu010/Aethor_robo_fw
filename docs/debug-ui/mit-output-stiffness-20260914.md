# MIT 输出坐标增益匹配与额定转矩分阶段验证

用户确认 7 号电机输出端空载，并同意先确认 MIT 运动稳定，再将调试反馈保护阈值从 1 Nm 调整到额定 3.5 Nm。

厂家《3. 电机上手流程(三)-MIT模式说明》第 1 页给出位置误差项、速度误差项和前馈力矩相加的公式。第 2 页明确 Kd 过大同样可能振荡，不能把示例电机的 Kp/Kd 直接视为本台 S3519 的标定值。S3519 V1.1 手册额定扭矩为 3.5 Nm，峰值为 7.8 Nm。

此控制器的现场证据表明，打包位置与 p_m 随转子变化，速度字段采用输出侧单位。LCD 已将输出位置乘 Gr=19.2032，但仍下发原始 Kp=40。按照当前兼容坐标契约，要使位置误差项以输出轴弧度计算，必须使 Kp_wire=Kp_output/Gr。否则 Kp=40 对输出坐标的等效位置增益为 768.128，而不是 40。此为控制契约匹配修正；它是否足以解决实际跟踪异常仍必须经过硬件试验。

第一阶段：LCD 保持输出坐标 Kp=40；线上的请求、内部校验范围及 STOP/HOLD 增益统一为约 2.083。发布给 LCD 的增益再乘 Gr，使显示与输入一致。Kd=0.2、前馈转矩=0、默认速度=10°/s、角度步长=1°不变，仍保留 1 Nm 调试保护。先进行 +1°、2°/s 单次空载试验，通过后再逐步验证 +10°、10°/s。

第二阶段：只有在空载跟踪稳定、有实际反馈和失能证据后，才将保护阈值设为额定 3.5 Nm。这是反馈超限后停机阈值，不是硬性电流/转矩钳位，也不等于将驱动器 TMAX 编码范围改成 3.5。

前一版本的触发样本：-1.1159954 Nm、-58.76°/s，对应正向 10°/s 请求。不能用提高保护阈值本身作为控制问题已解决的证据。故障恢复按钮及触发样本保留功能继续有效。

## 第一阶段烧录与现场记录

2026-09-14 16:54 归档 `D:/download/TCG/output/mit-output-stiffness-20260914/20260914T165438`：27 项检查通过，四个 Keil 目标均为 0 错误、0 警告；验证期间一方源码哈希未变化。HEX SHA256 为 `375b24b5f12a986830205a4740d0d207cde745e0647594c89b18bf6073c9837a`，249760 字节 Flash 完整回读一致，保存了旧固件 1 MiB 备份。烧录前后 STOP 均确认失能；重启 boot=3270486294，LCD 输入、显示及协议健康。

运行读回内部 Kp=2.0829861、LCD Kp=39.999996，确认换算生效。随后已有一次 LCD 请求 2：MIT 输出 +10°、10°/s，接受时间 31036000 us，35146000 us 以 MOTION/TIMEOUT 结束，失能确认；本次未触发 MIT 反馈保护。此时未采集完整运动曲线，不能判定跟踪稳定。后续 LCD STOP 请求 3 成功，并再次取得本地控制。已安排单次 +1°、2°/s 同步只读采样，3.5 Nm 阈值尚未应用。

当前 VMAX=200 的 12 位速度字段每档约 5.60°/s，2°/s 请求不能由速度反馈精确分辨；小动作以位置轨迹和时间为主要观察依据，不能把量化速度显示当作精确测速。

## 小动作采样与下一版修正

用户报告 +1°、2°/s “动了一下，然后显示动作失败”。归档 `mit-live-capture-20260914T165911.json` 共 127 个采样，边界处不一致的动作身份样本需剔除。请求 7 最终以到位超时结束并确认失能。位置由 6.19154 转子 rad 变为约 6.28691 rad，即输出位移约 0.285°，与目标仍差约 0.715°；采样峰值转矩约 0.501 Nm，没有触发 1 Nm 保护。未采样到的瞬时值不作为已知峰值。

检查发现 POS 到位、进度及行程余量和 MIT 到位比较仍使用转子位置差，原来 0.5° 被收紧为约 0.026° 输出角度。下一版将这些 LCD 比较统一到输出角度，USB 维持原始坐标。新增测试确认 LCD 0.49°可进入到位判据、0.51°不可，以及 USB 判据不变；旧实现测试失败，修正后七种应用配置通过。

小动作实际残差 0.715°仍大于正确的0.5°到位容差，因此不能把单位判据修正作为该动作必然通过的证明。输出 Kp 由40试调为80（线端约4.166），Kd和前馈不变，继续保持1 Nm保护。该值为待验证调试值，不能标为厂家建议或已标定。额定3.5 Nm部署仍待稳定跟踪证据。

该修正版已烧录至归档 `D:/download/TCG/output/mit-output-arrival-20260914/20260914T171126`。27项检查全部通过、四目标0错误0警告，HEX SHA256=`d9f8b5646b5d1501413ec0682d2a96e7c59ffb5dbae94cda3f73f4ff400ffabd`，249840字节完整回读一致。旧固件逐字节匹配并备份1MiB；使用与上一归档完全相同的坐标证据（并非新的Gr测量），烧录前后另行执行新鲜身份/STOP失能检查。重启boot=2478635101，LCD Kp≈80、内部Kp≈4.166，输入显示及协议健康。已启动一次小角度动作采样。

现场审计 `motor7-failure-audit-20260914T091539.json`：请求2为+5°、2°/s，完成；请求3为+10°、6°/s，完成；请求6为+1°、2°/s，1.072秒完成。三次均确认失能，无MIT保护触发。请求4为+10°、10°/s，接受后12毫秒因位置范围拒绝，此时累计位置接近正向端点，不能解释为转矩/跟踪失败。最后输出位置约+33.5°。第一次采集只捕获前一动作尾部1个样本，不能作为上述动作的完整曲线；已安排负向−10°、10°/s只读采样，避免越过正向位置上限。

用户确认负向测试移动成功。`mit-live-capture-20260914T171751.json` 捕获请求9的80个采样（79个动作身份一致），请求为−10°、10°/s；主要运动段实际输出位移约−9.87°，最后已采样位置与目标差约0.127°，采样最大转矩绝对值约0.589 Nm。速度字段量化样本为−0.440至+0.147 rad/s，不可宣称严格无超调或精确恒速。`motor7-failure-audit-20260914T091844.json` 确认请求9及随后+10°、10°/s请求10都完成、确认失能，无MIT保护触发。正向请求10没有完整运动曲线，不冒充双向连续采样。

基于上述有界空载验证进入额定阈值阶段：将反馈转矩停止门限从1 Nm改为3.5 Nm，保持输出Kp=80、Kd=0.2、前馈0及位置/速度/温度保护。正负3.5 Nm边界和正负3.501 Nm超限均由软件注入测试覆盖，不安排实机制造超转矩。3.5 Nm是本电机额定转矩选取的反馈保护阈值，不能等同于瞬时硬钳位或带载能力验收。

## 最终额定阈值版本

已烧录归档 `D:/download/TCG/output/mit-rated-torque-20260914/20260914T172850`。HEX SHA256=`c55bca02f9dabae20263f4df16707875e9324192bccbd041cdebc571ea34d326`，AXF SHA256=`5dec3be1a46d3c76ddfc84409389431ab3b72ad088fd332cafde8e4dbbff92ca`；249848字节Flash完整回读一致，旧固件匹配并备份1MiB。最终27项检查通过，四目标0错误0警告，正负3.5 Nm允许和正负3.501 Nm触发保护的测试通过。重启boot=3329205618，STOP确认motor7失能、fault=0、主控idle；LCD输入显示协议健康，输出位置约13.55°、Kp约80，内部Kp约4.166。

本次最终烧录没有自动下发运动命令。门限调整前的成功移动与采样属于上一份归档，同一增益和到位修正保留在最终版。后续LCD可重新申请本地控制，MIT相对±10°、10°/s调试；绝对目标必须留在约±37.30°协议输出范围内。不要把最大相对跨度约74.59°误解为从任意当前位置都可增加该角度。故障后可用中键STOP/重新检查刷新失能反馈后再申请控制。
